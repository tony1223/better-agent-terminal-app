// APP_VERSION is replaced at build time by the release workflows
// (.github/workflows/release-aab.yml and release-testflight.yml) from the
// pushed git tag. Local/dev builds show 'dev'.
export const APP_VERSION = 'dev'
